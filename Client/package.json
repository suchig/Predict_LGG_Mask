{
  "name": "ImageSegmentClient",
  "version": "1.0.0",
  "description": "Web front end to predict mask",
  "private": true,
  "scripts": {
    "test": "react-scripts test.js --env=jsdom",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws s3 sync build/ s3://serverless-imagesegment-app --acl public-read"
  },
  "dependencies": {
    "konva": "^4.0.5",
    "parcel-bundler": "^1.12.3",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-hot-loader": "^4.12.10",
    "react-konva": "^16.9.0-0",
    "react-scripts": "^3.1.1",
    "serialize-to-js": "^3.0.0",
    "utif": "^3.1.0"
  },
  "devDependencies": {},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
